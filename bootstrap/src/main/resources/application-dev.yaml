spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/admin
    username: admin
    password: secret

app:
  cors:
    allowed-origins: http://localhost:4200
  auth-server:
    url: http://localhost:9000
    realm: crm-realm
    client-id: crm-api-client
    client-secret: u9Ukq7VL4SXCyStfGjdY9RJHSOpvYDRB

server:
  port: 8000
  servlet:
    context-path: /api
